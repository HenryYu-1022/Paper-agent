# Agent Notes

- If the repository root contains `CLAUDE.md` or `claude.md`, read it before exploring the codebase. Treat it as the first source of project-specific commands, architecture, and workflow context so we keep future sessions lean.
- Do not overwrite or revert unrelated local changes. This repository often has active work in progress.
